# Pakistan Property - APIs (no trailing slash)
# Listings/Properties data: live admin API (Listings + Fraud Dashboard isi se fetch)
REACT_APP_PROPERTIES_API_URL=https://admin.pakistanproperty.com
# Admin par list ke liye GET route (backend jo support kare, e.g. /api/properties/list)
REACT_APP_PROPERTIES_LIST_ENDPOINT=/api/properties/list
# Optional: local Laravel for submit listing
# REACT_APP_FRAUD_API_URL=http://127.0.0.1:8000
